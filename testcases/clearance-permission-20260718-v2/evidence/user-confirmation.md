模拟交易和正式交易的核心规则、操作和预期一致时，合并为同一个 TC，并使用两个平级入口分支覆盖。
普通建仓人和普通跟进人的目标范围包括：自己、所有下级、直属上级、直属下级。
普通建仓人取 role_type∈{10,20}，关系包括等于、不等于、包含于、不包含、完全包含于。
普通跟进人取 role_type=30，关系包括等于、不等于、包含于、不包含、完全包含于。
建仓人（指定用户）和跟进人（指定用户）的关系包括包含于、不包含，并支持追加多个用户。
建仓人=自己、建仓人≠直属上级等具体关系与目标组合必须进入行为测试场景。
配置项存在性验证不能代替配置行为验证。
用户仅看到权限命中的股票行，未命中股票行不可见，汇总行只统计可见股票。
清仓策略和清后重建策略段独立控权。
多个新旧权限条件支持 AND 和 OR 连接，原有权限保留。
可用资金弹窗不受本次清仓股权限变更影响。
指定用户场景覆盖单个用户、追加多个用户、其中一个用户命中和所有用户均不命中。
不得扩写重复用户自动去重、重复用户不可选择、重复用户提示或删除用户后的保存规则。
本轮不修改业务代码，不执行真实页面、接口或数据库 SQL。
CONF-001 确认：不包含关系只要命中不包含条件就可见，未命中则不可见；多用户任一命中时可见，全部未命中时不可见。
